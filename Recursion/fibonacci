#include<iostream>
using namespace std;
int func(int n)
{
    if(n<2) return n;
    return func(n-1)+func(n-2);
}
int main()
{
    int n;
    cin>>n;
    for(int i=0;i<=n;i++)
    cout<<func(i)<<" ";
    
}
